name: hoowlib
description: "A new Flutter project."
publish_to: 'none'
version: 0.7.0+8

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  table_calendar: ^3.2.0
  sqflite: ^2.3.0
  path: ^1.8.0
  url_launcher: ^6.2.5
  package_info_plus: ^8.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  android: true
  ios: true
  image_path: 'assets/icon/hoowliblogo.png'

flutter:
  uses-material-design: true
  generate: true
